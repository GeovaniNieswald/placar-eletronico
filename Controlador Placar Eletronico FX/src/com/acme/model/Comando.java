package com.acme.model;

public enum Comando {

    // Comandos
    ESCOLHER_ESPORTE(), VERIFICAR_USUARIO_PLACAR(), TEXTO_INFERIOR(), CADASTRO_USUARIO(),
    NOME_TIME(), PONTOS(), FALTAS(), IMAGENS(), PROPAGANDA(), CRONOMETRO(), PERIODO(), BONUS(), POSSE(), DESCONECTAR();
}
